# Local Setup

## Prerequisites
- Node.js (version from `.nvmrc` or `package.json` engines)
- npm
- Git
- VS Code (recommended)

## Clone & Install
```bash
git clone <repo-url>
cd wbe-onecart-ui-refactor
npm install

## if the package installation falils use 

npm install --legacy-peer-deps

## don't use --force at the end